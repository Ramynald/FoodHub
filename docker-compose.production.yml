version: '3.3'

volumes:
  pg_data:
  static:
  media:

services:
  db:
    image: postgres:15
    restart: always
    env_file: .env
    volumes:
      - pg_data:/var/lib/postgresql/data

  backend:
    image: gramulechka/foodhub_backend:latest
    restart: always
    env_file: .env
    volumes:
      - static:/app/static/
      - media:/app/media/
    depends_on:
      - db

  frontend:
    image: gramulechka/foodhub_frontend:latest
    command: cp -r /app/build/. /frontend_static/
    volumes:
      - static:/frontend_static

  gateway:
    image: gramulechka/foodhub_gateway:latest
    restart: always
    volumes:
      - static:/usr/share/nginx/html/
      - media:/media/
    ports:
      - "80:80"
    depends_on:
      - backend
